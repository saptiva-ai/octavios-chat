# =============================================================================
# Production Environment Variables - TEMPLATE
# =============================================================================
#
# INSTRUCTIONS:
# 1. Copy this file: cp envs/.env.prod.example envs/.env.prod
# 2. Replace all placeholder values with your real production values
# 3. NEVER commit envs/.env.prod to the repository
#
# SECURITY:
# - This file is safe to commit (contains NO real credentials)
# - envs/.env.prod is in .gitignore and should NEVER be committed
# - Use git-secrets or gitleaks to prevent accidental commits
#
# =============================================================================

# -----------------------------------------------------------------------------
# Production Server Configuration
# -----------------------------------------------------------------------------

# Production server IP address
# Example: 1.2.3.4
PROD_SERVER_IP=YOUR_SERVER_IP_HERE

# SSH user for deployment
# Example: ubuntu
PROD_SERVER_USER=YOUR_SSH_USER_HERE

# Full SSH connection string (user@ip)
# Example: ubuntu@1.2.3.4
PROD_SERVER_HOST=${PROD_SERVER_USER}@${PROD_SERVER_IP}

# Absolute path to deployment directory on server
# Example: /home/ubuntu/octavios-chat
PROD_DEPLOY_PATH=YOUR_DEPLOY_PATH_HERE

# Production domain
# Example: chat.yourcompany.com
PROD_DOMAIN=YOUR_DOMAIN_HERE

# -----------------------------------------------------------------------------
# Database Credentials (Production)
# -----------------------------------------------------------------------------

# MongoDB password for production
# SECURITY: Use a strong password (min 16 chars, alphanumeric + symbols)
MONGODB_PASSWORD=YOUR_MONGODB_PASSWORD_HERE

# MongoDB username (usually same as development)
MONGODB_USERNAME=octavios_user

# MongoDB database name
MONGODB_DATABASE=octavios

# -----------------------------------------------------------------------------
# Application Secrets (Production)
# -----------------------------------------------------------------------------

# JWT Secret Key for token signing
# SECURITY: Generate with: openssl rand -hex 32
JWT_SECRET_KEY=YOUR_JWT_SECRET_HERE

# Saptiva API Key
# Get from: https://saptiva.ai/dashboard/api-keys
SAPTIVA_API_KEY=YOUR_SAPTIVA_API_KEY_HERE

# Saptiva Base URL
SAPTIVA_BASE_URL=https://api.saptiva.ai

# -----------------------------------------------------------------------------
# Optional: External Services
# -----------------------------------------------------------------------------

# Aletheia Research API Key (if using deep_research tool)
# ALETHEIA_API_KEY=YOUR_ALETHEIA_KEY_HERE

# SMTP Configuration (for email notifications)
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASSWORD=your-app-password

# -----------------------------------------------------------------------------
# MinIO S3 Storage (Production)
# -----------------------------------------------------------------------------

# MinIO access credentials
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=YOUR_MINIO_PASSWORD_HERE

# MinIO endpoint (internal)
MINIO_ENDPOINT=minio:9000

# MinIO public host (for presigned URLs)
# Use your production domain or IP
MINIO_DEFAULT_PUBLIC_HOST=http://${PROD_DOMAIN}:9000

# -----------------------------------------------------------------------------
# Redis Configuration (Production)
# -----------------------------------------------------------------------------

# Redis password
# SECURITY: Use a strong password
REDIS_PASSWORD=YOUR_REDIS_PASSWORD_HERE

# Redis host (usually 'redis' in docker-compose)
REDIS_HOST=redis

# Redis port
REDIS_PORT=6379

# -----------------------------------------------------------------------------
# Qdrant Vector Database (Production)
# -----------------------------------------------------------------------------

# Qdrant host (usually 'qdrant' in docker-compose)
QDRANT_HOST=qdrant

# Qdrant HTTP port
QDRANT_PORT=6333

# Qdrant gRPC port
QDRANT_GRPC_PORT=6334

# Qdrant API Key (optional, for authentication)
# QDRANT_API_KEY=YOUR_QDRANT_API_KEY_HERE

# -----------------------------------------------------------------------------
# Application Configuration
# -----------------------------------------------------------------------------

# Environment
ENVIRONMENT=production

# Debug mode (should be false in production)
DEBUG=false

# Log level
LOG_LEVEL=INFO

# Allowed origins for CORS (comma-separated)
ALLOWED_ORIGINS=https://${PROD_DOMAIN},https://www.${PROD_DOMAIN}

# -----------------------------------------------------------------------------
# Docker Compose Project Name
# -----------------------------------------------------------------------------

# Project name for docker-compose (affects container names)
COMPOSE_PROJECT_NAME=octavios-chat

# -----------------------------------------------------------------------------
# Security Notes
# -----------------------------------------------------------------------------
#
# 1. NEVER commit envs/.env.prod to git
# 2. Use strong passwords (16+ chars, alphanumeric + symbols)
# 3. Rotate secrets regularly (every 90 days recommended)
# 4. Use different secrets for development and production
# 5. Store production secrets in a secure vault (e.g., 1Password, AWS Secrets Manager)
# 6. Limit access to production secrets (need-to-know basis)
# 7. Enable audit logging for secret access
#
# -----------------------------------------------------------------------------
# Verification Checklist
# -----------------------------------------------------------------------------
#
# Before deploying to production, verify:
# [ ] All YOUR_*_HERE placeholders replaced with real values
# [ ] MONGODB_PASSWORD is strong (16+ chars)
# [ ] JWT_SECRET_KEY generated with openssl rand -hex 32
# [ ] SAPTIVA_API_KEY is valid and has sufficient quota
# [ ] PROD_DOMAIN points to your production server
# [ ] ALLOWED_ORIGINS includes your production domain
# [ ] DEBUG=false
# [ ] ENVIRONMENT=production
# [ ] envs/.env.prod is in .gitignore (verify with: git check-ignore envs/.env.prod)
#
# =============================================================================
